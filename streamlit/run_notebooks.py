import nbformat
from nbconvert.preprocessors import ExecutePreprocessor

# notebook_filename = 'your_notebook.ipynb'  # Replace with your notebook's path
# with open(notebook_filename) as f:
#     nb = nbformat.read(f, as_version=4)

# ep = ExecutePreprocessor(timeout=600, kernel_name='python3')
# # You can adjust 'timeout' (in seconds) and 'kernel_name' as needed.

# # Execute the notebook, optionally specifying a path for metadata
# ep.preprocess(nb, {'metadata': {'path': './'}})

# output_notebook_filename = 'executed_notebook.ipynb'
# with open(output_notebook_filename, 'w', encoding='utf-8') as f:
#     nbformat.write(nb, f)
